import 'package:get/get.dart';

/// Settings screen binding
class SettingsBinding extends Bindings {
  @override
  void dependencies() {
    // Theme and Locale are already global, but we can add specific ones here
  }
}
